[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# Next.js handles its own headers via next.config.ts — no duplicate here.

[[plugins]]
  package = "@netlify/plugin-nextjs"
